# global options
{
	admin off
	persist_config off
	auto_https off
	log {
		format json
	}
	servers {
		trusted_proxies static private_ranges 100.0.0.0/8
	}
}

# site block: listen on all interfaces so Railway can reach the app
0.0.0.0:{$PORT:5173} {
	log {
		format json
	}

	rewrite /health /*
	root * dist
	encode gzip
	file_server
	try_files {path} /index.html
}
